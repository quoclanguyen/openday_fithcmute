# Một số module cần cài đặt trước khi sử dụng:
### Chạy các lệnh sau:
* `pip install pygame`
* `pip install pillow`
* `pip install numpy`

# Chạy file main.py để khởi chạy chương trình:
`python main.py`
